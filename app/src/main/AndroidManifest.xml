<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bytes.wit.showcasing">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="bytes.wit.application.ShowCasingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:configChanges="locale|keyboardHidden|screenSize|orientation"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DemoActivity" />
        <activity
            android:name=".StoreLocatorActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/text_store_location" />

        <service android:name="bytes.wit.services.ProductProviderService" />

        <activity android:name=".StoreLocatorDetailActivity"
            android:parentActivityName=".StoreLocatorListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".StoreLocatorListActivity" />

        </activity>
    </application>

</manifest>